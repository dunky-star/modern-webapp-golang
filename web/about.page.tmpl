{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{if .Data.Title}}{{.Data.Title}}{{else}}About Us Page{{end}}</h1>
            </div>
        </div>
    </div>
{{end}}