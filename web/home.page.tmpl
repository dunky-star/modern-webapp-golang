{{template "base" .}}

{{define "content"}}
    <div class="container">
        <div class="row">
            <div class="col">
                <h1>{{if .Data.Title}}{{.Data.Title}}{{else}}Welcome to the Home Page{{end}}</h1>
                <p>This is the home page of the application.</p>
            </div>
        </div>
    </div>
{{end}}
